#include "DAQService.idl"

enum HB {
	ZERO,
	ONE
};

interface HeartBeatService
{
	RTC::ReturnCode_t setOperatorToComp(in HB hbs);
	HB getOperatorToComp();
	RTC::ReturnCode_t setCompToOperator(in HB hbs);
	HB getCompToOperator();
	DAQDone checkDone();
	void    setDone();
};